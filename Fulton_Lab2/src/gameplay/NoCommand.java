package gameplay;

/**
 * This will be our default Command so we do not have to check every 
 * time if a slot is empty or not.
 * @author Moumouni Noma
 */
public class NoCommand implements Command 
{ 
	@Override
	public void execute(int r, int c) 
	{
		
	}
}